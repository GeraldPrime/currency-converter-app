allprojects {
    ext.kotlin_version = '1.8.0' // Use the version compatible with your libraries
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"l
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
